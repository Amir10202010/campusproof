# Доступность и скорость — 2026-09-18

> P4 · #40. Страницы: главная `/` и профиль `/u/Q1046224` (KIMEP University), плюс локальный профиль-фикстура `/search?q=demo&replay=1`.

## Lighthouse

**Числа Lighthouse пока не сняты.** Анонимная квота Google PageSpeed Insights API на 18.09 исчерпана (HTTP 429), а `npx lighthouse` скачивает пакет — это делаем только с разрешения человека. Как снять за 2 минуты:

- Chrome → DevTools → вкладка Lighthouse → режим «Навигация», устройство «Мобильное», категории Performance, Accessibility, Best practices → для https://campusproof.vercel.app/ и https://campusproof.vercel.app/u/Q1046224;
- или `npx lighthouse <url> --only-categories=performance,accessibility,best-practices --form-factor=mobile --quiet --output=json` (после разрешения).

| Страница | Доступность (цель ≥ 95) | Лучшие практики (≥ 90) | Скорость (≥ 80) | LCP | CLS |
|---|---|---|---|---|---|
| `/` | [заполнить] | [заполнить] | [заполнить] | [заполнить] | [заполнить] |
| `/u/Q1046224` | [заполнить] | [заполнить] | [заполнить] | [заполнить] | [заполнить] |

## Ручная проверка: до и после исправлений

| Проверка | До | После |
|---|---|---|
| Кольцо фокуса (`ring-ring/50`) против белого фона, контраст | 1,54 : 1 — почти не видно | **3,41 : 1** (цель WCAG для не-текста ≥ 3 : 1) |
| Вторичный текст (`text-muted-foreground`) на белом / на сером фоне `bg-muted` | 4,73 / **4,34 : 1** — на сером ниже 4,5 | **6,00 / 5,50 : 1** |
| Бейджи уровней (6 сочетаний: 3 уровня × светлая и тёмная тема) | ≥ 4,5 : 1 (тест с #36) | без изменений, ≥ 4,5 : 1 |
| Esc закрывает диалог доказательств | да | да |
| Фокус после закрытия диалога (Esc или крестик) | уходил на `body` — Radix возвращает фокус только на `Dialog.Trigger`, а диалог открывается из карточки | **возвращается на ту же карточку** |
| `prefers-reduced-motion: reduce` | крутилки, пульсации, выезд диалога, плавная прокрутка, анимация зума карты | всё выключено: глобальное правило в `globals.css`, у карты `zoomAnimation`/`fadeAnimation` выключены, «Показать» прокручивает без анимации |
| Прыжки вёрстки: карточки фото | `aspect-4/3`, без прыжков | без изменений |
| Прыжки вёрстки: большое фото в диалоге | высота появлялась после загрузки | место резервируется по `width`/`height` фото, если они известны |
| `alt` у картинок (профиль-фикстура) | 26 из 26 | 26 из 26; логотипы в списке выбора — `alt=""` (рядом есть название) |
| Кнопки и ссылки без доступного имени | 0 | 0 (переключатель «Показывать неподтверждённые» назван через `<label for>`) |
| Повторяющиеся `id` | 0 | 0 |
| Язык, заголовки, ориентиры | `lang="ru"`, один `h1`, header / nav / main / footer, ссылка «Перейти к содержимому» | без изменений |
| Уровни не только цветом | значок + текст на бейджах, легенда карты текстом | без изменений |

Контраст считается по токенам темы (OKLCH → sRGB → WCAG), смешивание кольца 50 % — в sRGB, как у браузера; всё закреплено тестами `tests/ui-tiers-contrast.test.ts`.

## Замечания к чужим зонам

Новых нет. Производительность прода сейчас больше всего зависит от #83 и #84: профили деградированные и не кешируются, поэтому каждый просмотр — новый прогон пайплайна.
