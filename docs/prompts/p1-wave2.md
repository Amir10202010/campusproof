# P1 · Пайплайн и интеграция — волна 3 (вечер пятницы)

Ты — AI-агент роли **P1 «Пайплайн и интеграция»**. Прочитай `docs/agent-playbook.md`, дальше работай по списку ниже самостоятельно. Заморозка функций прошла: **новых возможностей не добавляем, только исправления и подготовка к сдаче** (суббота, 12:00).

**Твоя зона:** `lib/resolver/**`, `lib/sources/{wikidata,wikipedia,commons,geo,wikimediaFetch}.ts`, `lib/describe/**`, `lib/cache/**`, `lib/pipeline/**`, `lib/client/**`, `hooks/**`, `components/containers/**`, `app/api/**`, `app/search/**`, `app/u/**`, `lib/types.ts`, `package.json`, `.github/**`, `.claude/**`, `vercel.json`.

## По порядку

1. **#96 — Gemini не принимает ключ.** Самый дорогой баг: без него нет ни визуальной проверки, ни сгенерированного описания. В задаче расписана диагностика и команда проверки ключа. Код: повтори в `lib/describe/description.ts` подход из `lib/vision/gemini.ts` — прямой REST с `x-goog-api-key`, текст ошибки API в сообщении (SDK при отсутствии ключа молча уходит в OAuth и отдаёт непонятный 401).
2. **#85 — Commons не успевает для больших вузов** (ETH Zurich): профиль остаётся без фото. Урезать обход подкатегорий по бюджету времени и отдавать частичный результат вместо таймаута.
3. **#86 — резолвер не находит «ЕНУ» и Karaganda Buketov University.** Аббревиатуры и варианты названий — в индексе и в `queryVariants`.
4. **#87 — `Photo.geo.distanceToCampusM` не заполняется**, из-за этого в доказательствах нет расстояния.
5. **#92 — счётчики фильтров обнуляются** при выборе категории.
6. **#10 и #11** — прогон пайплайна на живых данных: тайминги, медиана до 30 секунд, страница `/u/[qid]`.
7. **#26 (слой B, по желанию)** — reuse-детектор на Redis. Браться **только если всё выше закрыто**: незаконченный модуль перед сдачей придётся убирать из `deps.ts`.

## Перед сдачей (суббота, до 10:30)
- `/api/health` зелёный, `configured` — все нужные сервисы `true`;
- `node scripts/qa/smoke.mjs --base https://campusproof.vercel.app` без FAIL;
- удалить `app/api/dev/sse-check`, убрать из `lib/pipeline/deps.ts` источники, которые так и не реализованы (чтобы в интерфейсе не было «не подключён»);
- `npm run build` и финальный прогон 3 вузов.
