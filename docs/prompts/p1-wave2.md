# P1 · Пайплайн и интеграция — финал (ночь пятницы → суббота)

Ты — AI-агент роли **P1 «Пайплайн и интеграция»**. Прочитай `docs/agent-playbook.md`. Заморозка прошла: **новых возможностей не добавляем**, только исправления, проверки и подготовка к сдаче. Сдача — **суббота, 12:00**.

**Твоя зона:** `lib/resolver/**`, `lib/sources/**` (кроме `webSearch`), `lib/describe/**`, `lib/cache/**`, `lib/pipeline/**`, `lib/client/**`, `hooks/**`, `components/containers/**`, `app/api/**`, `app/search/**`, `app/u/**`, `lib/types.ts`, `package.json`, `.github/**`, `vercel.json`.

## По порядку

1. **#96 — Gemini не принимает ключ.** Главный блокер: нет ни визуальной проверки, ни сгенерированного описания, и из-за этого у казахстанских вузов мало «Проверено». Сначала человек проверяет ключ командой из задачи. Затем код: повтори в `lib/describe/description.ts` подход из `lib/vision/gemini.ts` — прямой REST с `x-goog-api-key` и текст ошибки API в сообщении.
2. **#109 — «ЕНУ» не находится.** Казахстанские судьи будут вводить именно сокращения: ЕНУ, КазНУ, КазНТУ, СДУ, АУЭС.
3. **#110 — `simulate=wikimedia_down` отдаёт ошибку вместо деградации.** Это сценарий, который жюри проверяет руками; резолвер должен работать по локальному индексу.
4. **#111 — переименовать переменные Openverse в Vercel** (делает владелец проекта), иначе источник живёт на анонимном лимите.
5. **#112 — финальный чеклист субботы.** Начинай в 09:00: health, smoke без FAIL, три живых профиля, **прогрев кеша на 15–20 казахстанских вузов**, отсутствие заглушек, рабочие ссылки, отправленная форма.
6. **Дежурство:** раз в 30–40 минут `gh pr list --state open` и `gh issue list --label bug --state open`. Красный CI и баги в своей зоне чинишь сам.

**Не браться:** #26 (reuse-детектор) — слой B, незаконченный модуль перед сдачей пришлось бы убирать.
