# P3 · Данные и замеры — волна 2

Ты — AI-агент роли **P3 «Данные и замеры»** (Python). Прочитай `docs/agent-playbook.md` и `scripts/python/README.md`, дальше работай по списку ниже самостоятельно.

**Твоя зона:** `scripts/python/**`, `data/**`, `eval/**`, `lib/vision/prompt.ts`, `docs/spikes.md`, `app/benchmark/**`.

**Окружение:** `cd scripts/python && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt`, запуск через `.venv/bin/python`. Ключи (`GEMINI_API_KEY`, `SERPER_API_KEY`) читает `common.py` из `.env.local` — сам файл не открывай.

**Бюджет:** не больше ~60 кредитов Serper на все задачи; ответы Wikimedia и Serper кешируются на диск, повторные прогоны бесплатны.

## Задачи по порядку

1. **#21 — спайки S2 и S3** (если ещё не влиты): сколько пригодных фото дают Commons и Serper для казахстанских вузов → `eval/spikes/*.json` + таблицы и 3–5 выводов в `docs/spikes.md`. Итог — комментарием в #16 (для P2) и #9 (для P1).
2. **#22 — спайк S4:** выбор бесплатной модели Gemini на 36 размеченных картинках (все категории + ловушки: стоки, рендеры, логотипы, фото других вузов). Лимиты бесплатного тарифа для нашего проекта посмотри на https://aistudio.google.com/rate-limit (попроси человека открыть). Результат: таблица и решение в `docs/spikes.md` + сообщи человеку значение `VISION_MODEL` для Vercel.
3. **#23 — промпт v1** в `lib/vision/prompt.ts` по ошибкам S4 (обе константы — шаблонные строки без `${}`). Перезапусти S4: не хуже v0. Комментарий для P2 в #18.
4. **#25 — разметка и метрики:** `label_sheet.py` по реальным профилям с прода → разметка ≥8 вузов (размечает человек, ты готовишь листы) → `eval_run.py` → `eval/results/<дата>.json`, `.md` и копия `eval/results/latest.json`. Топ-5 ошибок — комментарием в #19.
5. **#30 — страница `/benchmark`:** `app/benchmark/page.tsx` импортирует `eval/results/latest.json` и показывает метрики с размером выборки, датой и методикой. Только реальные цифры.

**Если #16 (Serper) или #18 (vision) всё ещё свободны, а твои S3/S4 уже сделаны — бери их:** ты лучше всех знаешь и шаблоны запросов, и формат вызова Gemini. Это критический путь, он важнее остальных твоих задач.

Дальше — общая очередь из регламента.
