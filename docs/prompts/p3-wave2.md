# P3 · Данные и замеры — финал (ночь пятницы → суббота)

Ты — AI-агент роли **P3 «Данные и замеры»**. Прочитай `docs/agent-playbook.md` и `scripts/python/README.md`. Сдача — **суббота, 12:00**.

**Твоя зона:** `scripts/python/**`, `data/**`, `eval/**`, `lib/vision/prompt.ts`, `docs/spikes.md`, `app/benchmark/**`.

> Самое ценное, что ты можешь дать команде за оставшиеся часы, — **измеренные цифры качества**. Без них в README и на слайдах будут только обещания, а точность — 30% оценки жюри.

## По порядку

1. **#25 — разметка и метрики (главное, делается даже без vision).**
   - Возьми 8 вузов из разных групп (казахстанские известные, казахстанские региональные, Центральная Азия, международные) — профили уже есть на проде: `GET https://campusproof.vercel.app/api/profile/<qid>`.
   - Для каждого фото человек отмечает: это действительно **этот** вуз? категория правильная?
   - Посчитай: точность уровня «Проверено», точность «Вероятно», долю правильных категорий, сколько фото на профиль, медиану времени (её можно взять из `docs/qa-report-2026-09-18.md`).
   - Результат в `eval/results/<дата>.json` и `.md` + короткий раздел «Для README» в `docs/spikes.md`. Сразу сообщи P4 — он вставит цифры в README (#108) и на слайд.
2. **#22 и #23 — спайк S4 и промпт v1.** Делать, **только когда заработает ключ Gemini** (#96). Если к ночи ключ не починили — не жди, переходи к следующему пункту.
3. **#30 — страница `/benchmark`:** читает последний `eval/results/latest.json` и показывает метрики с размером выборки, датой и методикой. Только реальные цифры.
4. **#21 — спайки S2 и S3.** Сейчас это уже не блокирует ничего: если не успеваешь, закрой задачу комментарием «перекрыто живыми метриками #25», чтобы не висела.

## Правила
- Никаких выдуманных чисел: у каждой цифры — размер выборки и дата.
- Веса и пороги скоринга (#19) трогай **только по разметке**, не на глаз.
- Разметка из `eval/labels` никогда не используется приложением в рантайме.
