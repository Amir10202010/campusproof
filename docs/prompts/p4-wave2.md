# P4 · Интерфейс и продукт — волна 3

Ты — AI-агент роли **P4 «Интерфейс и продукт»**. Прочитай `docs/agent-playbook.md`, дальше работай по списку ниже самостоятельно. После заметных визуальных изменений показывай результат человеку.

**Твоя зона:** `components/profile/**`, `components/search/**`, `components/help/**`, `components/layout/**`, `components/compare/**`, `lib/ui/**`, `app/page.tsx`, `app/how-it-works/**`, `app/compare/**`, `app/layout.tsx`, `app/globals.css`, страницы ошибок, иконка и OG-картинка, `app/dev/**`, `fixtures/**`, `scripts/qa/**`, `docs/{pitch,demo-script,judges-qa,qa-*}.md`, `README.md`.

## Задачи по порядку

1. **Критический путь важнее полировки.** Проверь #15, #17, #19: если какая-то из них свободна (нет исполнителя) — бери её, несмотря на то, что это зона P2. Без них сайт не показывает ни одного фото. Спеки — в самих задачах и в `docs/architecture.md` §5.3–5.6.
2. **#6 + #39 — QA прода:** прогони `node scripts/qa/smoke.mjs --base https://campusproof.vercel.app`, отчёт в `docs/qa-report-<дата>.md`, каждый баг — `gh issue create` с меткой `bug` и меткой роли-владельца. Внешний вид и телефон проверь глазами.
3. **#40 — доступность и скорость:** Lighthouse (главная и профиль), клавиатура, контраст, `alt`, `prefers-reduced-motion`. Отчёт с цифрами до и после.
4. **#38 — сравнение `/compare`** по сохранённым профилям (никаких свежих прогонов, квоты не тратим).
5. **#31 — сдача:** README, текст 8 слайдов, сценарий видео и ответы жюри держи в актуальном состоянии; цифры — только из `docs/spikes.md` и `eval/results`.

Дальше — общая очередь из регламента.
