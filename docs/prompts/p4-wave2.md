# P4 · Интерфейс и продукт — финал (ночь пятницы → суббота)

Ты — AI-агент роли **P4 «Интерфейс и продукт»**. Прочитай `docs/agent-playbook.md`. Заморозка прошла: **интерфейс больше не меняем**, кроме явных багов. Сдача — **суббота, 12:00**.

**Твоя зона:** `components/**` (кроме `containers`), `lib/ui/**`, `app/page.tsx`, `app/how-it-works/**`, `app/compare/**`, `app/layout.tsx`, страницы ошибок, `scripts/qa/**`, `docs/{pitch,demo-script,judges-qa,qa-*}.md`, `README.md`.

> Код продукта готов. Всё, что осталось у тебя, — это материалы, по которым жюри будет нас оценивать. Без видео и слайдов работа не засчитывается.

## По порядку

1. **#106 — видео до 3 минут.** Сценарий по секундам в `docs/demo-script.md`. Записывать **с прода**, этап поиска не ускорять, показать доказательства, честный пустой раздел и один отказ через `?simulate=web_search_down`. Загрузить, проверить ссылку в инкогнито.
2. **#107 — 8 слайдов в PDF.** Текст готов в `docs/pitch.md`. На слайде «Результаты» — только измеренные цифры с размером выборки (бери из `docs/qa-report-2026-09-18.md`, а если P3 успеет — из `eval/results`).
3. **#108 — README: настоящие цифры, имена команды, ссылки** на видео, слайды и прод. Сейчас там `TODO` и `_имя_`.
4. **Свежий smoke перед сдачей:** `node scripts/qa/smoke.mjs --base https://campusproof.vercel.app`, отчёт закоммитить, найденные баги — issues владельцам.
5. **#40 — доступность:** быстрый прогон Lighthouse по главной и профилю, цифры до/после в отчёт. Только если пункты 1–4 закрыты.

## Правила
- Никаких новых экранов и функций: чиним только то, что мешает демонстрации.
- Цифры — только измеренные, с размером выборки и датой.
